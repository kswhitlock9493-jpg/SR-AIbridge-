# ==========================
# RENDER BACKEND ENVIRONMENT TEMPLATE
# SOVEREIGN DOMINION TOKEN FORGE INTEGRATION v1.9.7s
# ==========================
# Copy this file to .env.render (for local testing) or set these variables
# in the Render Dashboard for production deployment.
# DO NOT commit .env.render with actual secrets!
#
# All secrets are managed by the Sovereign Dominion Token Forge.
# Run: bash runtime/pre-deploy.dominion.sh to mint ephemeral tokens

# Forge Dominion Configuration (REQUIRED)
# Set FORGE_DOMINION_ROOT in GitHub Secrets or Render Environment Variables
FORGE_DOMINION_MODE=sovereign
FORGE_DOMINION_VERSION=1.9.7s

# Core Database Connection - MANAGED BY PLATFORM
# Render automatically provides this in production
DATABASE_URL=<PLATFORM_MANAGED>
DATABASE_TYPE=postgres

# Backend API Configuration
# This should match your Render service URL
BRIDGE_API_URL=https://sr-aibridge.onrender.com
PORT=8000
ENVIRONMENT=production

# Security & Authentication - MANAGED BY FORGE DOMINION
# These secrets are auto-generated by Token Forge - DO NOT set manually
SECRET_KEY=<FORGE_MANAGED>
FEDERATION_SYNC_KEY=<FORGE_MANAGED>

# Bridge Services
# Update these if using different frontend URLs
VAULT_URL=https://bridge.netlify.app/api/vault
CASCADE_MODE=production

# Logging Configuration
# Options: debug, info, warn, error
LOG_LEVEL=info

# CORS Configuration
CORS_ALLOW_ALL=false
ALLOWED_ORIGINS=https://bridge.netlify.app,https://sr-aibridge.netlify.app

# Render Service Configuration - MANAGED BY FORGE DOMINION
# These are used by automation scripts and minted by Token Forge
RENDER_API_KEY=<FORGE_MANAGED>
RENDER_SERVICE_ID=srv-d39k3ejuibrs73etqnag

# Diagnostics (Internal Genesis telemetry only)
AUTO_DIAGNOSE=true
DIAGNOSE_WEBHOOK_URL=

# Debug Mode (disable in production)
DEBUG=false

