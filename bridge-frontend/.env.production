# ==========================================================================================
# SR-AIbridge Frontend Production Environment
# BRH (Bridge Runtime Handler) + Netlify Integration
# ==========================================================================================
# This file provides production defaults for the Netlify deployment
# These can be overridden by environment variables in the build process

# API Configuration - BRH Backend (Self-Hosted)
# For BRH deployments, the backend runs locally on the same machine
# Default: localhost:8000 (can be overridden via GitHub Secrets or netlify.toml)
VITE_API_BASE=http://localhost:8000
REACT_APP_API_URL=http://localhost:8000

# Bridge Configuration
CASCADE_MODE=production
VAULT_URL=https://sr-aibridge.netlify.app/api/vault
BRIDGE_API_URL=http://localhost:8000

# Forge Dominion Configuration
FORGE_DOMINION_MODE=sovereign
FORGE_DOMINION_VERSION=2.0.0
