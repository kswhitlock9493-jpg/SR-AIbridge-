# ==========================================================================================
# SR-AIbridge Frontend Environment Template
# SOVEREIGN DOMINION TOKEN FORGE INTEGRATION v1.9.7s + BRH Integration
# ==========================================================================================
# All secrets managed by Token Forge - DO NOT add plaintext secrets
# Replace <PLACEHOLDER> values with actual URLs from your deployment

# API Configuration
# For local development with BRH (Bridge Runtime Handler)
VITE_API_BASE=http://localhost:8000
REACT_APP_API_URL=http://localhost:8000

# BRH Backend Configuration (for Vite proxy during development)
VITE_BRH_BACKEND_URL=http://localhost:8000
VITE_BRH_BACKEND_WS=ws://localhost:8000

# WebSocket Base URL for real-time updates
VITE_WS_BASE=ws://localhost:8000

# Bridge Configuration
CASCADE_MODE=production
VAULT_URL=https://sr-aibridge.netlify.app/api/vault
# For BRH deployment, update with your BRH URL
BRIDGE_API_URL=http://localhost:8000

# Environment
NODE_ENV=production

# Forge Dominion Configuration
# All secrets are managed by Token Forge on the backend
# Frontend does not require FORGE_DOMINION_ROOT
FORGE_DOMINION_MODE=sovereign
FORGE_DOMINION_VERSION=1.9.7s

