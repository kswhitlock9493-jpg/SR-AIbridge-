# ==========================
# PRODUCTION ENVIRONMENT VARIABLES
# SOVEREIGN DOMINION TOKEN FORGE INTEGRATION
# Source of truth for BRH (Bridge Runtime Handler) and Netlify deployments
# v2.0.0 - Full Sovereign BRH Environment
# ==========================

# Backend API Configuration (BRH-managed)
# Set BACKEND_URL or BRIDGE_URL in your environment to override
BRIDGE_API_URL=${BACKEND_URL:-${BRIDGE_URL:-https://bridge.sr-aibridge.com}}

# Frontend API Configuration
PUBLIC_API_BASE=/api
VITE_API_BASE=${BACKEND_URL:-${BRIDGE_URL:-https://bridge.sr-aibridge.com}}
REACT_APP_API_URL=${BACKEND_URL:-${BRIDGE_URL:-https://bridge.sr-aibridge.com}}

# Bridge Configuration
CASCADE_MODE=production
VAULT_URL=https://sr-aibridge.netlify.app/api/vault

# Federation & Security - MANAGED BY FORGE DOMINION
# DO NOT set plaintext secrets here - use Token Forge
# Run: bash runtime/pre-deploy.dominion.sh to mint tokens
FEDERATION_SYNC_KEY=<FORGE_MANAGED>

# Diagnostics (Internal Genesis telemetry only)
AUTO_DIAGNOSE=true

# Forge Dominion Configuration
# Set FORGE_DOMINION_ROOT in GitHub Secrets or runtime environment
# All provider tokens are minted at runtime via Token Forge (SDTF)
FORGE_DOMINION_MODE=sovereign
FORGE_DOMINION_VERSION=2.0.0

# BRH (Bridge Runtime Handler) Configuration
# Sovereign deployment using self-hosted Docker infrastructure
BRH_ENABLED=true
BRH_ALLOW_UNSIGNED=false

# ===== Genesis Framework - Full Engine Activation =====
# engines_enable_true: Permanent Full Activation Protocol
# All engines start by default under RBAC & Truth certification
ENGINES_ENABLE_TRUE=true
GENESIS_MODE=enabled
LINK_ENGINES=true
BLUEPRINTS_ENABLED=true

# RBAC and Safety
RBAC_ENFORCED=true
# ENGINE_SAFE_MODE enables security features: RBAC enforcement, truth certification, safe engine operations
# This is DIFFERENT from SAFE_PLACEHOLDER_MODE (used in CI/CD workflows for testing)
# ENGINE_SAFE_MODE should always be 'true' in production - it's a security feature, not a limitation
ENGINE_SAFE_MODE=true
AUTO_HEAL_ON=true
TRUTH_CERTIFICATION=true

# ===== Bridge Resonance & Sovereignty =====
# System harmony and readiness gate for production deployment
BRIDGE_RESONANCE=0.99
SOVEREIGNTY_ENABLED=true
SOVEREIGNTY_TIMEOUT=30.0
# Production thresholds for stable deployment
SOVEREIGNTY_MIN_PERFECTION=0.75
SOVEREIGNTY_MIN_HARMONY=0.85
SOVEREIGNTY_MIN_RESONANCE=0.70
SOVEREIGNTY_MIN_OVERALL=0.75
