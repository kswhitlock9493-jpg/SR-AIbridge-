# ==========================
# SR-AIBRIDGE DEPLOY CONFIG
# SOVEREIGN DOMINION TOKEN FORGE INTEGRATION
# All secrets managed by Token Forge - NO PLAINTEXT SECRETS
# ==========================

# --- Backend / Render ---
# Database credentials managed by Render platform
DATABASE_URL=<PLATFORM_MANAGED>
CASCADE_MODE=production
VAULT_URL=https://sr-aibridge.netlify.app/api/vault
FEDERATION_SYNC_KEY=<FORGE_MANAGED>
AUTO_DIAGNOSE=true

# Render API credentials - MANAGED BY FORGE DOMINION
RENDER_API_KEY=<FORGE_MANAGED>
RENDER_SERVICE_ID=srv-sr-aibridge-main

# --- Frontend / Netlify ---
VITE_API_BASE=https://sr-aibridge.onrender.com/api
REACT_APP_API_URL=https://sr-aibridge.onrender.com/api

# --- Infrastructure Stabilization ---
BRIDGE_ENV=production
BRIDGE_DIAGNOSTICS=https://diagnostics.sr-aibridge.com
BRIDGE_BACKEND=https://bridge.sr-aibridge.com
BRIDGE_RENDER=https://sr-aibridge.onrender.com
NETLIFY_API=https://api.netlify.com
CONFIDENCE_MODE=enabled

# Forge Dominion Configuration
# Set FORGE_DOMINION_ROOT in GitHub Secrets
# All provider tokens are minted at runtime via Token Forge
FORGE_DOMINION_MODE=sovereign
FORGE_DOMINION_VERSION=1.9.7s
