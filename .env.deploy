# ==========================
# SR-AIBRIDGE DEPLOY CONFIG
# SOVEREIGN DOMINION TOKEN FORGE INTEGRATION
# All secrets managed by Token Forge (SDTF) - NO PLAINTEXT SECRETS
# ==========================

# --- Backend / BRH (Bridge Runtime Handler) ---
# Database credentials managed by BRH runtime or platform
DATABASE_URL=<PLATFORM_MANAGED>
CASCADE_MODE=production
VAULT_URL=https://sr-aibridge.netlify.app/api/vault
FEDERATION_SYNC_KEY=<FORGE_MANAGED>
AUTO_DIAGNOSE=true

# BRH Configuration
BRH_ENABLED=true
BRH_ALLOW_UNSIGNED=false

# --- Frontend / Netlify ---
# Backend URL can be overridden with BACKEND_URL or BRIDGE_URL
VITE_API_BASE=${BACKEND_URL:-${BRIDGE_URL:-https://bridge.sr-aibridge.com}}/api
REACT_APP_API_URL=${BACKEND_URL:-${BRIDGE_URL:-https://bridge.sr-aibridge.com}}/api

# --- Infrastructure Stabilization ---
BRIDGE_ENV=production
BRIDGE_DIAGNOSTICS=https://diagnostics.sr-aibridge.com
BRIDGE_BACKEND=https://bridge.sr-aibridge.com
NETLIFY_API=https://api.netlify.com
CONFIDENCE_MODE=enabled

# Forge Dominion Configuration
# Set FORGE_DOMINION_ROOT in GitHub Secrets or runtime environment
# All provider tokens are minted at runtime via Token Forge (SDTF)
FORGE_DOMINION_MODE=sovereign
FORGE_DOMINION_VERSION=2.0.0

