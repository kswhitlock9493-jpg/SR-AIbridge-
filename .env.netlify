# Bridge Frontend Configuration
# Frontend-safe environment values for Netlify deployment
# These values are safe to expose in frontend builds and do not trigger secret scans

# API Base URL - relative path for Netlify proxy
PUBLIC_API_BASE=/api

# CASCADE_MODE controls bridge operation mode
# Options: development, production
# Use 'production' for deployed services
CASCADE_MODE=production

# VAULT_URL - API endpoint for vault operations
VAULT_URL=https://bridge.netlify.app/api/vault

# Optional Monitoring
# Leave empty if not using Datadog monitoring
DATADOG_API_KEY=
DATADOG_REGION=us
