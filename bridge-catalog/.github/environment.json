{
  "version": "1.9.6t",
  "description": "SR-AIbridge Environment Configuration",
  "last_sync": "2025-10-12T03:00:00Z",
  "variables": {
    "AUTONOMY_ENABLED": {
      "description": "Enables self-healing system",
      "required": true,
      "default": "true"
    },
    "AUTONOMY_API_TOKEN": {
      "description": "Auth key for API-triggered incidents",
      "required": true,
      "secret": true
    },
    "PUBLIC_API_BASE": {
      "description": "API base URL",
      "required": true
    },
    "GITHUB_TOKEN": {
      "description": "For secret creation",
      "required": true,
      "secret": true
    },
    "GITHUB_REPOSITORY": {
      "description": "Owner/repo for GitHub API calls",
      "required": true
    },
    "FEDERATION_SYNC_KEY": {
      "description": "Cross-engine handshake key",
      "required": false,
      "secret": true
    },
    "BLUEPRINT_MODE": {
      "description": "Blueprint operation mode",
      "required": false,
      "default": "predictive",
      "options": ["predictive", "adaptive"]
    },
    "TRUTH_API_KEY": {
      "description": "Validates certificate authenticity",
      "required": false,
      "secret": true
    }
  }
}
